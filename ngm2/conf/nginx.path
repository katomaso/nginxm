	location {{PATH}} {
		root /var/www/{{domain}}{{path}};
		client_max_body_size 3M;
		index index.html;
		{% if authfile %}
		auth_basic "restricted";
		auth_basic_user_file {{authfile}};
		{% else %}
		auth_basic off;
		{% endif %}
	}